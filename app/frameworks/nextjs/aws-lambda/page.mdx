import { Callout, Steps, Tabs, Cards } from "nextra/components";

export const metadata = {
  title: "Deploy Next.js to AWS Lambda",
};

# Deploy Next.js to AWS Lambda

Pros:

- Great for preview/PR environments.
- You only pay for what you use.
- Can handle spiky traffic well.

Cons:

- Possible issues with libraries like i18n for translations.
- Can be hard to predict costs.
